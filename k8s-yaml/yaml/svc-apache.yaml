---

apiVersion: v1
kind: Service
metadata:
  name: svc-apache
spec:
  selector:
    app: apache
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30005
  type: NodePort
